import java.util.Random;
import java.util.Random;
/*
public class LineBattleRevivedButNotReallyEnumVersion {
   WeaponChoice enemyWeaponChoice() {
      Random random = new Random();
      int choice = random.nextInt(3);
      if (choice == 0)
        return WeaponChoice.BOMB;
      else if (choice == 1)
          return WeaponChoice.SHOOT;
      else if (choice == 2)
        return WeaponChoice.TRAP;
      else
        return null;
    }

    public static void main(String[] args) {
      LineBattleRevivedButNotReallyEnumVersion prg = new LineBattleRevivedButNotReallyEnumVersion();
      WeaponChoice weaponChoice;
      weaponChoice = prg.enemyWeaponChoice();

      switch (weaponChoice) {
        // 0 = Bomb
        case BOMB:
          enemyPlaceBomb();
          break;
        // 1 = Shoot
        case 1:
          enemyShoot();
          break;
        // 2 = Trap
        case 2:
          enemyPlaceTrap();
          break;
      }
    }
  }

}


 */