public class Switch {
     /* switch (dice) {

      case 1:
        playerMovement = 1;
        System.out.println("Do you want to move " + playerMovement + "F or B?");
        movementChoice = in.nextLine();
        if (movementChoice.contains("F")) {
          distance--;
          System.out.println("The distance between you and the enemy is now " + distance);
        }
        else if (movementChoice.contains("B")) {
          distance++;
          System.out.println("The distance between you and the enemy is now " + distance);

        }
        break;

      case 2:
        playerMovement = 2;
        System.out.println("Do you want to move " + playerMovement + "F or B?");
        movementChoice = in.nextLine();
        if (movementChoice.contains("F")) {
          distance -= 2;
          System.out.println("The distance between you and the enemy is now " + distance);
        }
        else if (movementChoice.contains("B")) {
          distance += 2;
          System.out.println("The distance between you and the enemy is now " + distance);
        }
        break;

      case 3:
        playerMovement = 1;
        System.out.println("Do you want to move " + playerMovement + "F or B?");
        movementChoice = in.nextLine();
        if (movementChoice.contains("F")) {
          distance --;
          System.out.println("The distance between you and the enemy is now " + distance);
        }
        else if (movementChoice.contains("B")) {
          distance ++;
          System.out.println("The distance between you and the enemy is now " + distance);
        }
        break;

      case 4:
        playerMovement = 2;
        System.out.println("Do you want to move " + playerMovement + "F or B?");
        movementChoice = in.nextLine();
        if (movementChoice.contains("F")) {
          distance -= 2;
          System.out.println("The distance between you and the enemy is now " + distance);
        }
        else if (movementChoice.contains("B")) {
          distance += 2;
          System.out.println("The distance between you and the enemy is now " + distance);
        }
        break;

      case 5:
        playerMovement = 1;
        System.out.println("Do you want to move " + playerMovement + "F or B?");
        movementChoice = in.nextLine();
        if (movementChoice.contains("F")) {
          distance --;
          System.out.println("The distance between you and the enemy is now " + distance);
        }
        if (movementChoice.contains("B")) {
          distance ++;
          System.out.println("The distance between you and the enemy is now " + distance);
        }
        break;

      case 6:
        playerMovement = 2;
        System.out.println("Do you want to move " + playerMovement + "F or B?");
        movementChoice = in.nextLine();
        if (movementChoice.contains("F")) {
          distance -= 2;
          System.out.println("The distance between you and the enemy is now " + distance);
        }
        if (movementChoice.contains("B")) {
          distance += 2;
          System.out.println("The distance between you and the enemy is now " + distance);
        }
        break;

    }*/
}
